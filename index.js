
// Write your code here

// equation to equal 62 for the variable multiply
let num1 = 31;
let num2 = 2;
let multiply = (num1 * num2); //does not require parenthesis
multiply;

//generates a random integer greater than 0

let random = Math.ceil(Math.random() * 10);

//is an equation that calulates a remainder that is equal to 4
let num3 = 104;
let num4 = 5;
let mod = num3 % num4; 
mod;

//will return 20 as the highest number in the set
let max = Math.max(5, 10, 15, 20);